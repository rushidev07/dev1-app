<?php
/** @var \Ahy\PlpRevamp\Block\Brands\AllBrands $block */
$brands    = $block->getAllBrands();
$count     = count($brands);
$bannerUrl = $block->getBannerUrl();
?>
<style>
    .ahy-brands-page {
        --plp-blue:     #12334b;
        --plp-divider:  #e5e7eb;
        --plp-card-pad: 8px;
    }
    /* Section background behind the page title + cards (scoped to this page via body class) */
    body.ahy-brands-body #maincontent,
    body.ahy-brands-body .column.main {
        background: #EFEADD;
    }
    .ahy-brands-page { padding-bottom: 48px; }
    .ahy-brands-page__banner { margin-bottom: 24px; }
    .ahy-brands-page__banner-img { width: 100%; height: auto; display: block; }
    .ahy-brands-page__count {
        font-size: 0.875rem;
        font-weight: 600;
        color: var(--plp-blue);
        margin: 0 0 16px;
    }
    .ahy-brands-page__empty {
        color: #666;
        text-align: center;
        padding: 48px 0;
    }
    .ahy-brands-page__grid {
        list-style: none;
        padding: 0;
        margin: 0;
        display: grid;
        grid-template-columns: repeat(1, 1fr);
        gap: 16px;
        align-items: start;
    }
    .ahy-brands-page__link {
        display: block;
        text-decoration: none;
        color: inherit;
    }
    .ahy-brands-page__img-wrap {
        aspect-ratio: 360 / 315;
        overflow: hidden;
        background: #fff;
        display: flex;
        align-items: center;
        justify-content: center;
    }
    .ahy-brands-page__img {
        width: 100%;
        height: 100%;
        object-fit: contain;
        /* padding: var(--plp-card-pad); */
    }
    .ahy-brands-page__placeholder {
        width: 100%;
        height: 100%;
        display: flex;
        align-items: center;
        justify-content: center;
    }
    .ahy-brands-page__placeholder svg { stroke: var(--plp-blue); }
    .ahy-brands-page__name {
        color: var(--plp-blue);
        padding-top: 8px;
        font-size: 1.125rem;
        font-weight: 700;
        text-transform: capitalize;
        line-height: 1.4rem;
        display: -webkit-box;
        -webkit-line-clamp: 2;
        line-clamp: 2;
        -webkit-box-orient: vertical;
        overflow: hidden;
        min-height: 2.8rem;
        word-break: break-word;
        overflow-wrap: break-word;
        text-align: center;
        margin: 0;
    }
    .ahy-brands-page__item:hover .ahy-brands-page__name { text-decoration: underline; text-underline-offset: 6px; }

    @media (min-width: 768px)  { .ahy-brands-page__grid { grid-template-columns: repeat(2, 1fr); } }
    @media (min-width: 1024px) { .ahy-brands-page__grid { grid-template-columns: repeat(4, 1fr); } }
</style>

<?php if ($bannerUrl): ?>
<div class="ahy-brands-page__banner" role="img" aria-label="<?= $block->escapeHtmlAttr(__('All Brands Banner')) ?>">
    <img src="<?= $block->escapeUrl($bannerUrl) ?>"
         alt="<?= $block->escapeHtmlAttr(__('All Brands')) ?>"
         class="ahy-brands-page__banner-img"
         loading="eager" />
</div>
<?php endif; ?>

<div class="ahy-brands-page container">

    <p class="ahy-brands-page__count">
        <?= $block->escapeHtml(__('%1 brands', $count)) ?>
    </p>

    <?php if (empty($brands)): ?>
        <p class="ahy-brands-page__empty">
            <?= $block->escapeHtml(__('No brands found.')) ?>
        </p>
    <?php else: ?>
        <ul role="list" class="ahy-brands-page__grid">
            <?php foreach ($brands as $brand): ?>
            <li class="ahy-brands-page__item">
                <a href="<?= $block->escapeUrl($brand['url']) ?>" class="ahy-brands-page__link">
                    <div class="ahy-brands-page__img-wrap">
                        <?php if ($brand['image']): ?>
                            <img loading="lazy"
                                 src="<?= $block->escapeUrl($brand['image']) ?>"
                                 alt="<?= $block->escapeHtmlAttr($brand['label']) ?>"
                                 width="360"
                                 height="315"
                                 class="ahy-brands-page__img" />
                        <?php else: ?>
                            <div class="ahy-brands-page__placeholder">
                                <svg xmlns="http://www.w3.org/2000/svg" width="64" height="64" fill="none"
                                     viewBox="0 0 24 24" stroke-width="1">
                                    <path stroke-linecap="round" stroke-linejoin="round"
                                          d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z" />
                                </svg>
                            </div>
                        <?php endif; ?>
                    </div>
                    <div class="ahy-brands-page__name-wrap">
                        <h3 class="ahy-brands-page__name">
                            <?= $block->escapeHtml($brand['label']) ?>
                        </h3>
                    </div>
                </a>
            </li>
            <?php endforeach; ?>
        </ul>
    <?php endif; ?>

</div>
