<?php
/**
 * Caliber Nation — Inline Rejoin (STATE C — Cancelled member)
 *
 * Logged-in ex-member clicks "Rejoin", confirms in one step, membership
 * is reactivated without leaving the account page.
 *
 * IMPORTANT: Single root <div> required — Magewire attaches wire:id here.
 *
 * @var \Magento\Framework\View\Element\Template          $block
 * @var \Magento\Framework\Escaper                        $escaper
 * @var \Ahy\CaliberNation\Magewire\Account\MembershipRejoin $magewire
 */

use Hyva\Theme\Model\ViewModelRegistry;
use Hyva\Theme\ViewModel\HeroiconsOutline;

/** @var HeroiconsOutline $heroicons */
$heroicons = $block->getData('view_models')
    ? $block->getData('view_models')->require(HeroiconsOutline::class)
    : null;
?>

<div>

    <?php if ($magewire->rejoined): ?>
    <!-- ── Success: auto-reload to show STATE A ──────────────────────────────── -->
    <div class="flex flex-col items-center text-center py-6"
         x-data="{show:false}"
         x-init="requestAnimationFrame(()=>{show=true}); setTimeout(()=>window.location.reload(), 1500)"
         x-show="show"
         x-transition:enter="transition ease-out duration-500"
         x-transition:enter-start="opacity-0 scale-95"
         x-transition:enter-end="opacity-100 scale-100">

        <div class="flex items-center justify-center w-16 h-16 rounded-full bg-green-100 mb-5">
            <svg class="w-8 h-8 text-green-600" fill="none" stroke="currentColor"
                 viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5"
                      d="M5 13l4 4L19 7"/>
            </svg>
        </div>

        <h3 class="font-league-gothic text-3xl text-ahy-blue mb-2 uppercase">
            <?= $escaper->escapeHtml($magewire->withinPaidThrough
                ? __('Cancellation Undone')
                : __('Welcome Back!')) ?>
        </h3>
        <p class="text-sm text-gray-500 mb-2">
            <?= $escaper->escapeHtml(__('Your Caliber Nation membership is active again.')) ?>
        </p>
        <p class="text-xs text-gray-400">
            <?= $escaper->escapeHtml(__('Loading your membership...')) ?>
        </p>
    </div>

    <?php else: ?>

    <!-- ── Error message ─────────────────────────────────────────────────────── -->
    <?php if ($magewire->errorMessage): ?>
    <div class="flex items-start gap-3 mb-5 p-4 bg-red-50 border border-red-200 rounded-lg text-sm text-red-700">
        <svg class="w-5 h-5 shrink-0 mt-0.5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
            <path stroke-linecap="round" stroke-linejoin="round"
                  d="M12 9v2m0 4h.01M10.29 3.86L1.82 18a2 2 0 001.71 3h16.94a2 2 0 001.71-3L13.71 3.86a2 2 0 00-3.42 0z"/>
        </svg>
        <div>
            <?= $escaper->escapeHtml($magewire->errorMessage) ?>
        </div>
    </div>
    <?php endif; ?>

    <!-- ── Rejoin CTA — Alpine manages confirmation panel, zero server round-trip ── -->
    <div x-data="{ confirm: false }">

        <?php if ($magewire->withinPaidThrough): ?>
        <!--
            Cancelled BUT still inside the paid-through term: benefits have NOT
            stopped (MemberAccess keeps cancelled members entitled until
            renewal_date), so the copy must reassure rather than sell a "rejoin to
            regain access" that isn't needed.
        -->
        <h2 class="font-league-gothic text-3xl sm:text-4xl text-ahy-blue uppercase leading-tight mb-3">
            <?= $escaper->escapeHtml(__('Your Benefits Are Still Active')) ?>
        </h2>
        <p class="text-gray-500 text-sm leading-relaxed mb-4">
            <?= $escaper->escapeHtml(__("You've cancelled your Caliber Nation membership, so auto-renew is off and you won't be charged again.")) ?>
        </p>

        <!-- Reassurance callout — the date their access actually runs until -->
        <div class="flex items-start gap-3 p-4 bg-green-50 border border-green-200 rounded-lg mb-6">
            <svg class="w-5 h-5 text-green-600 shrink-0 mt-0.5" fill="none" viewBox="0 0 24 24"
                 stroke="currentColor" stroke-width="2">
                <path stroke-linecap="round" stroke-linejoin="round" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/>
            </svg>
            <p class="text-sm text-green-800">
                <?= $escaper->escapeHtml(__('You keep every member benefit — member pricing, early-bird access and savings tracking — until %1.', $magewire->paidThroughDate)) ?>
            </p>
        </div>

        <p class="text-gray-500 text-sm leading-relaxed mb-8">
            <?= $escaper->escapeHtml(__('Changed your mind? Undo your cancellation and your membership goes back to Active.')) ?>
        </p>

        <!-- Undo-cancellation button (hidden when confirm panel is open) -->
        <button x-show="!confirm"
                @click="confirm = true"
                type="button"
                class="inline-flex items-center justify-center gap-2
                       w-full sm:w-auto
                       bg-ahy-red hover:bg-ahy-red-light
                       text-white font-bold rounded-full px-10 h-14 transition-colors">
            <?= $escaper->escapeHtml(__('Undo Cancellation')) ?>

        <?php else: ?>
        <h2 class="font-league-gothic text-3xl sm:text-4xl text-ahy-blue uppercase leading-tight mb-3">
            <?= $escaper->escapeHtml(__("We'd Love You Back")) ?>
        </h2>
        <p class="text-gray-500 text-sm leading-relaxed mb-4">
            <?= $escaper->escapeHtml(__('Your Caliber Nation membership has ended.')) ?>
        </p>
        <p class="text-gray-500 text-sm leading-relaxed mb-8">
            <?= $escaper->escapeHtml(__('Rejoin anytime and immediately regain access to all member benefits, exclusive pricing, and early-bird deals.')) ?>
        </p>

        <!-- Rejoin button (hidden when confirm panel is open) -->
        <button x-show="!confirm"
                @click="confirm = true"
                type="button"
                class="inline-flex items-center justify-center gap-2
                       w-full sm:w-auto
                       bg-ahy-red hover:bg-ahy-red-light
                       text-white font-bold rounded-full px-10 h-14 transition-colors">
            <?= $escaper->escapeHtml(__('Rejoin Caliber Nation')) ?>
        <?php endif; ?>
            <svg class="w-4 h-4 shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2.5">
                <path stroke-linecap="round" stroke-linejoin="round" d="M14 5l7 7m0 0l-7 7m7-7H3"/>
            </svg>
        </button>

        <!-- Inline confirmation panel — appears instantly via Alpine -->
        <div x-show="confirm"
             x-cloak
             x-transition:enter="transition ease-out duration-200"
             x-transition:enter-start="opacity-0 -translate-y-1"
             x-transition:enter-end="opacity-100 translate-y-0"
             class="rounded-xl border border-ahy-blue/20 overflow-hidden">

            <!-- Header bar -->
            <div class="flex items-center gap-2.5 px-5 py-5 bg-ahy-yellow-card border-b border-yellow-200">
                <svg class="w-5 h-5 text-ahy-blue shrink-0" fill="none" viewBox="0 0 24 24"
                     stroke="currentColor" stroke-width="2">
                    <path stroke-linecap="round" stroke-linejoin="round"
                          d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15"/>
                </svg>
                <p class="text-sm font-bold text-ahy-blue">
                    <?= $escaper->escapeHtml($magewire->withinPaidThrough
                        ? __('Undo your cancellation?')
                        : __('Reactivate your membership?')) ?>
                </p>
            </div>

            <!-- Body -->
            <div class="px-5 py-4 bg-white">
                <p class="text-xs text-gray-500 leading-relaxed mb-5">
                    <?php if (!$magewire->withinPaidThrough): ?>
                        <?= $escaper->escapeHtml(__('Your Annual Caliber Nation membership will be reactivated immediately. You will regain access to all member benefits, exclusive pricing, and early-bird deals.')) ?>
                    <?php elseif ($magewire->hasRenewalCard): ?>
                        <?= $escaper->escapeHtml(__('Your membership goes back to Active and will renew automatically on %1 using your saved card. Your paid-through date does not change and you will not be charged today.', $magewire->paidThroughDate)) ?>
                    <?php else: ?>
                        <?= $escaper->escapeHtml(__('Your membership goes back to Active through %1. Your paid-through date does not change and you will not be charged today — add a saved card afterwards if you want it to renew automatically.', $magewire->paidThroughDate)) ?>
                    <?php endif; ?>
                </p>

                <div class="flex flex-wrap items-center gap-3">

                    <!-- Confirm rejoin (Magewire) -->
                    <button wire:click="rejoin"
                            wire:loading.attr="disabled"
                            wire:target="rejoin"
                            type="button"
                            class="inline-flex items-center justify-center gap-2
                                   bg-ahy-blue hover:opacity-90
                                   text-white font-bold text-sm rounded-full px-6 h-11 transition-opacity
                                   disabled:opacity-60 disabled:cursor-not-allowed">
                        <span wire:loading.remove wire:target="rejoin"
                              class="inline-flex items-center gap-2">
                            <svg class="w-4 h-4 shrink-0" fill="none" viewBox="0 0 24 24"
                                 stroke="currentColor" stroke-width="2">
                                <path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7"/>
                            </svg>
                            <?= $escaper->escapeHtml($magewire->withinPaidThrough
                                ? __('Yes, Undo It')
                                : __('Yes, Rejoin')) ?>
                        </span>
                        <span wire:loading wire:target="rejoin" class="whitespace-nowrap">
                            <svg class="animate-spin h-4 w-4 inline-block align-middle mr-2" xmlns="http://www.w3.org/2000/svg"
                                 fill="none" viewBox="0 0 24 24">
                                <circle class="opacity-25" cx="12" cy="12" r="10"
                                        stroke="currentColor" stroke-width="4"/>
                                <path class="opacity-75" fill="currentColor"
                                      d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4z"/>
                            </svg><?= $escaper->escapeHtml($magewire->withinPaidThrough
                                ? __('Restoring...')
                                : __('Reactivating...')) ?>
                        </span>
                    </button>

                    <!-- Cancel (Alpine only) -->
                    <button @click="confirm = false"
                            type="button"
                            wire:loading.attr="disabled"
                            wire:target="rejoin"
                            class="inline-flex items-center justify-center
                                   border border-gray-200 hover:border-ahy-blue
                                   text-gray-500 hover:text-ahy-blue
                                   font-semibold text-sm rounded-full px-6 h-11 transition-all duration-150
                                   disabled:opacity-50 disabled:cursor-not-allowed">
                        <?= $escaper->escapeHtml(__('Not Yet')) ?>
                    </button>
                </div>
            </div>
        </div>

    </div>

    <?php endif; ?>

</div>
