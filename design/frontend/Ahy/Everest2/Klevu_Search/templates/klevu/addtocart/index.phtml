<?php /** @var $block \Klevu\Addtocart\Block\Index */ ?>

<?php if ($block->isAddtocartEnabled()): ?>
    <script type="text/javascript" id="ahyAddToCart">
        function klevu_addtocart(id, url, qty) {
            console.log('Add to cart button clicked for id: ' + id);return false;
            var el = document.createElement("a");
            el.setAttribute(
                "data-post",
                '{' +
                '"action": "<?= $block->escapeUrl($block->getUrl('checkout/cart/add')) ?>",' +
                '"data": {"product":"' + id + '", "qty":"' + qty + '"}' +
                '}'
            );
            el.style.width = "0px !important";
            el.style.height = "0px !important";
            document.body.appendChild(el);
            el.click();
            document.body.removeChild(el);
        }
    </script>
<?php endif; ?>
